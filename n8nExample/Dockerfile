FROM n8nio/n8n:1.111.0
USER root
# For Alpine-based images, use 'apk add'
RUN apk add --no-cache python3 py3-pip python3-venv 
# For Debian/Ubuntu-based images, use 'apt-get install'
# RUN apt-get update && apt-get install -y python3 python3-venv
USER node
